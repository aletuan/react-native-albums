import App from './App';
import { AppRegistry } from 'react-native';

// Render component into the device screen
//AppRegistry.registerComponent('albums', () => App);
AppRegistry.registerComponent('albums', () => App);
